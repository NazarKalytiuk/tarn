# Shared env defaults for the agent-loop example.
# Priority: --var > shell env > tarn.env.local.yaml > tarn.env.{name}.yaml >
# this file > the inline `env:` block in api.tarn.yaml.
base_url: "https://jsonplaceholder.typicode.com"
