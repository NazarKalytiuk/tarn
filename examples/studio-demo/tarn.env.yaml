# Default environment for the Studio demo. Other environments live in
# tarn.env.<name>.yaml siblings and are selectable from the Studio
# titlebar's env picker.

base_url: "https://jsonplaceholder.typicode.com"
httpbin:  "https://httpbin.org"
user_id:  "1"
